<div class="rps-sc-tile suf-tile-1c entry-content bottom">
    <div class="suf-gradient suf-tile-topmost"><h3>Showcase</h3></div>
    <div class="gallery gallery-columns-5 gallery-size-150">
        <div class="gallery-row gallery-row-equal">
            {% for image in images %}
                {% include 'inc/photo_gallery.html.twig' with {'url_popup': image.url_800, 'url_thumb': image.url_thumb} %}
            {% endfor %}
        </div>
    </div>
</div>
{% include 'inc/set_equal_height.js.twig' %}